# Phase 1: Full GSM8k training set with stronger reward signal
include: base.yaml
curriculum:
  split: train
  max_problems: null  # Use all training examples (~7.5k)
training:
  total_steps: 50000  # RL needs many iterations to explore and learn
