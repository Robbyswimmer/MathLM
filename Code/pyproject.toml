[project]
name = "mathlm"
version = "0.1.0"
description = "MathLM PPO training code"
authors = [{name = "MathLM Team"}]
requires-python = ">=3.11"
dependencies = [
    "transformers",
    "datasets",
    "accelerate",
    "trl",
    "wandb",
]

[tool.setuptools.packages.find]
where = ["src"]
