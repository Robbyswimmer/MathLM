# Dev Log – 2025-11-10

- Finalized project roadmap and repository organization plan aligned with updated course guidelines.
- Created mirrored documentation under `Code/docs/` for self-contained repo context.
- Scaffolded the MathLM codebase structure: `src/mathlm` package with submodules, `scripts/` entrypoints, `configs/` templates, experiment + data directories, docs, and placeholder tests.
- Added environment specification (`environment.yml`), packaging metadata (`pyproject.toml`), and repo-level README to streamline onboarding.
- Implemented GSM8k loader + curriculum filtering module and connected it to `scripts/prepare_data.py`, producing processed splits under `data/processed/` with metadata tracking.
- Added pytest coverage for curriculum filters and GSM8k load/save helpers to guard future changes.
- Built sandboxed Python verifier with keyword guards, reward calculator, and unit tests covering extraction, reward breakdowns, and execution safety.
- Added configuration parsing utilities, PPO training scaffold (`scripts/run_train.py`), evaluation stub, plotting script, and tests for config loading + data bootstrapping hooks.
- Added prompt dataset/JSONL logger/PPO stub modules with tests, and wired `scripts/run_train.py` to log rewards when TRL is unavailable.
- Upgraded PPO runner to support both stubbed logging and the TRL trainer path so real runs can reuse reward logic while still generating metrics when GPUs aren’t available.
- Added batched PPO runner logic (stub + TRL paths) so future training runs can collect rollouts, compute dense rewards, and log metrics per-step via JSONL.

## Next Steps
- Build sandboxed verifier + dense reward calculator in `mathlm.rewards` with accompanying tests.
- Wire up PPO training loop (`scripts/run_train.py`) using TRL and start Phase 1 experiments once data pipeline is verified.
- Implement evaluation + plotting utilities (`scripts/run_eval.py`, `scripts/make_plots.py`) and establish zero-/few-shot baselines.
